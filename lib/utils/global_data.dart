import 'package:flutter/material.dart';
import 'package:student_data_app/utils/student_modal.dart';

// List<Map<String, dynamic>> studentInfo = [];

List<StudentData> studentObj = [];
// studentInfo.map((element) => StudentData.fromMap(element)).toList();

Color primaryColor = Colors.indigo.shade100;
Color primaryTextColor = Colors.indigo.shade900;
